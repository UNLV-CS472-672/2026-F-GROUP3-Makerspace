legacy-peer-deps=true

# pnpm normally caches the *result* of dependency install scripts in its store
# and skips re-running them when the store is warm (e.g. restored from the CI
# cache). Cypress's binary and Prisma's generated client live outside the
# package folder, so that cache "hit" leaves them missing. Always re-run them.
side-effects-cache=false
